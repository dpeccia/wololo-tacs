language: java

jdk:
  - oraclejdk11

before_install:
  - chmod +x gradlew
  - chmod +x gradle/wrapper/gradle-wrapper.jar

script:
  - ./gradlew test build

after_success:
  - bash <(curl -s https://codecov.io/bash) -t d159f153-92a3-4c61-9767-028306713e3d